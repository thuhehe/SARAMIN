# Deferred sync findings

Things `/sync-doc` found in the build that the docs deliberately do NOT reflect
yet. One row per finding, newest first. This file exists so a skipped finding is
skipped **on the record** — the baseline moves past it, and it is still visible.

| Found | Repo@sha | What the build does | Doc target | Why not applied |
|---|---|---|---|---|
| — | — | — | — | — |
